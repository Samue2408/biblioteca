# Change these values before using the stack outside local development.
POSTGRES_DB=app
POSTGRES_USER=app
POSTGRES_PASSWORD=app

# Host ports (change them if they collide with another service).
POSTGRES_PORT=5432
BACKEND_PORT=8080
FRONTEND_PORT=3000
MAILHOG_SMTP_PORT=1025
MAILHOG_UI_PORT=8025
